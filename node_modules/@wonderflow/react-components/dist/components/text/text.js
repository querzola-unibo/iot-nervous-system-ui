import { jsx as _jsx } from "react/jsx-runtime";
import clsx from 'clsx';
import { forwardRef } from 'react';
import * as styles from './text.module.css';
export const Text = forwardRef(({ children, className, size, sentiment, dimmed, weight, maxWidth, textAlign = 'start', as: Wrapper = 'p', responsive = true, lineHeight = 'large', style, ...otherProps }, forwardedRef) => {
    const dynamicStyle = {
        '--max-w': maxWidth,
        '--t-align': textAlign,
    };
    return (_jsx(Wrapper, { ref: forwardedRef, "data-text-size": size, "data-text-weight": weight, "data-text-sentiment": sentiment, "data-text-dimmed": dimmed, "data-text-line-height": lineHeight, "data-text-responsive": size === 14 ? 14 : responsive, className: clsx(styles.Text, className), style: { ...dynamicStyle, ...style }, ...otherProps, children: children }));
});
